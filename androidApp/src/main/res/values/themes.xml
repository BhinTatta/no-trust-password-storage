<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all real UI theming (see ui/theme/Theme.kt). This
         exists only because the manifest needs some android:theme, and it
         should match the dark, near-black background before Compose even
         has a chance to draw, to avoid a white flash on cold start. -->
    <style name="Theme.NoTrustVault" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
